#gym-ponggers/setup.py
import gym
from setuptools import setup

setup(name='gym_ponggers',
      version='0.0.1',
      install_requires=['gym', 'pygame', 'numpy']  # And any other dependencies ponggers needs
)
